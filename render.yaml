{
  "build": {
    "commands": [
      "npm install",
      "npx prisma generate",
      "npm run build"
    ]
  },
  "deploy": {
    "startCommand": "node dist/src/server.js",
    "healthcheckPath": "/health",
    "env": [
      { "key": "NODE_ENV", "value": "production" }
    ]
  }
}
